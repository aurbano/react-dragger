{
  "name": "react-dragger-example",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://aurbano.eu/react-dragger/",
  "scripts": {
    "build:less": "./node_modules/.bin/lessc src/example.less src/example.css",
    "start": "npm run build:less && react-scripts start",
    "build": "npm run build:less && react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "postinstall": "linklocal"
  },
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-dragger": "file:../../",
    "react-scripts": "1.0.14"
  },
  "devDependencies": {
    "less": "^2.7.2"
  }
}
